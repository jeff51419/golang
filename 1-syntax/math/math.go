package main

import (
	"fmt"
	"math"
	"strings"
)

func main() {
	// math.Floor(2.75)
	// strings.Title("Head first go")
	fmt.Println(math.Floor(2.75))
	// Title 取得strings 把每個單字首字大寫處理

	fmt.Println(strings.Title("Head first go"))
}
